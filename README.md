# Telegram Whitelist Bot

Простой телеграм-бот для управления вайтлистом. Пользователи могут отправлять заявки на добавление в вайтлист со своим никнеймом, а администратор может одобрять или отклонять эти заявки.

## Возможности

- 📝 Пользователи могут подавать заявки на добавление в вайтлист
- ✅ Администратор может одобрять или отклонять заявки через удобный интерфейс
- 📊 Проверка статуса заявки
- 🔔 Автоматические уведомления для пользователей и администратора
- 💾 Хранение данных в SQLite

## Требования

- Go 1.21 или выше
- SQLite3

## Установка

1. Клонируйте репозиторий или скопируйте файлы проекта

2. Установите зависимости:
```bash
go mod download
```

3. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram и получите токен

4. Узнайте свой Telegram ID (можно через [@userinfobot](https://t.me/userinfobot))

5. Создайте файл `.env` или экспортируйте переменные окружения:
```bash
export BOT_TOKEN="your_bot_token_here"
export ADMIN_ID="your_telegram_id_here"
```

Или создайте файл `.env`:
```
BOT_TOKEN=your_bot_token_here
ADMIN_ID=your_telegram_id_here
```

## Запуск

### Вариант 1: Через переменные окружения

```bash
BOT_TOKEN="your_token" ADMIN_ID="your_id" go run .
```

### Вариант 2: С файлом .env (требуется установка godotenv)

```bash
go run .
```

### Вариант 3: Сборка и запуск

```bash
go build -o whitelist-bot
BOT_TOKEN="your_token" ADMIN_ID="your_id" ./whitelist-bot
```

## Использование

### Для пользователей:

- `/start` - Начало работы с ботом
- `/apply` - Подать заявку на вайтлист
- `/status` - Проверить статус заявки
- `/cancel` - Отменить текущее действие

### Для администратора:

- `/start` - Начало работы с ботом
- `/apply` - Подать заявку на вайтлист (доступно и админу)
- `/status` - Проверить статус заявки
- `/pending` - Просмотреть все ожидающие заявки
- `/cancel` - Отменить текущее действие

При появлении новой заявки администратор получает уведомление. Заявки можно одобрять или отклонять прямо в интерфейсе бота через inline-кнопки.

## Структура проекта

- `main.go` - Точка входа в приложение
- `config.go` - Загрузка конфигурации из переменных окружения
- `database.go` - Работа с базой данных SQLite
- `bot.go` - Логика бота и обработка команд
- `whitelist.db` - База данных SQLite (создается автоматически)

## База данных

База данных SQLite создается автоматически при первом запуске. Она содержит таблицу `whitelist_requests` со следующими полями:

- `id` - Уникальный идентификатор заявки
- `user_id` - Telegram ID пользователя
- `username` - Username пользователя в Telegram (если есть)
- `nickname` - Никнейм, указанный пользователем в заявке
- `status` - Статус заявки (pending/approved/rejected)
- `created_at` - Дата создания заявки
- `updated_at` - Дата последнего обновления

## Безопасность

- Убедитесь, что файл `.env` добавлен в `.gitignore`
- Никогда не коммитьте токены и ID в репозиторий
- Храните токен бота в безопасном месте

## Лицензия

MIT

